export function Header() {
    const headerStyle = {
        fontSize: '30px',
        backgroundColor: 'green',
    };
    return (
        <header style={headerStyle}>
            HEADER CONTENT
        </header>
    );
}